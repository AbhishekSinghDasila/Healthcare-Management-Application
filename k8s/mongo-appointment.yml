apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-appointment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-appointment
  template:
    metadata:
      labels:
        app: mongo-appointment
    spec:
      containers:
        - name: mongo-appointment
          image: mongo:7
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongo-appointment-storage
              mountPath: /data/db
      volumes:
        - name: mongo-appointment-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-appointment
spec:
  selector:
    app: mongo-appointment
  ports:
    - port: 27017
      targetPort: 27017
