apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-patient
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-patient
  template:
    metadata:
      labels:
        app: mongo-patient
    spec:
      containers:
        - name: mongo-patient
          image: mongo:7
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongo-patient-storage
              mountPath: /data/db
      volumes:
        - name: mongo-patient-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-patient
spec:
  selector:
    app: mongo-patient
  ports:
    - port: 27017
      targetPort: 27017
