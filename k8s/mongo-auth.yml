apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-auth
  template:
    metadata:
      labels:
        app: mongo-auth
    spec:
      containers:
        - name: mongo-auth
          image: mongo:7
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongo-auth-storage
              mountPath: /data/db
      volumes:
        - name: mongo-auth-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-auth
spec:
  selector:
    app: mongo-auth
  ports:
    - port: 27017
      targetPort: 27017
